services:

  gotify-webhooks:

    image: geroldpenz/gotify-webhooks

    build:
      context: .
      dockerfile: Dockerfile

    restart: unless-stopped

    environment:
      NPM_CONFIG_LOGLEVEL: "verbose"
#      GOTIFY_WEBSERVICE_BASE_URL: ""
#      GOTIFY_CLIENT_TOKEN: ""
