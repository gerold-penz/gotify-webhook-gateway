services:

  gotify-webhooks:

    image: geroldpenz/gotify-webhooks

    build:
      context: .
      dockerfile: Dockerfile

    restart: unless-stopped

    environment:
      NPM_CONFIG_LOGLEVEL: "verbose"
